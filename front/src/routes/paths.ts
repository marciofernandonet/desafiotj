export const rootPaths = {
  root: '/',
  authRoot: 'auth',
};

const paths = {
  root: rootPaths.root,
  livros: `/livros`,
  autores: `/autores`,
  assuntos: `/assuntos`,
  404: `/404`,
};

export default paths;
